sudo setfacl -m user:student:rw /var/run/docker.sock

# This commnad need to be run from cse user account

# Replace username with the username reflecting in your terminal, this will help to run docker without sudo privileges in current session